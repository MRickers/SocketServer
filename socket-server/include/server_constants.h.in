#pragma once
#include <array>

namespace socket_server {
	const size_t server_response_buffer_size = @SERVER_RESPONSE_BUFFER_SIZE@;

	using ResponseBuffer = std::array<uint8_t, server_response_buffer_size>;
}